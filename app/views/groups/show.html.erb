<%= render partial: "header" %>

<p><%= @group.description %></p>

<ul id="user-list">
  <% @users.each do |user| %>
    <li><%= user.name %></li>
  <% end %>
</ul>

<% if current_user.id == @group.admin.id %>
  <button><%= link_to "Edit", edit_group_path %></button>
<% end %>
